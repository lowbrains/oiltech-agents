# Сессия A — ядро MVP-1 в агентный main (23.09)

План — `docs/handoff_2026-09-21_single-contour-sessions.md`, раздел «Сессия A». Решение — ADR 0001.

**PR [#1](https://github.com/lowbrains/oiltech-agents/pull/1)**, ветка `merge/mvp1-core`, 4 коммита:
`bf34591` → `ffb8663` → `7479463` (слияния) → `028c9ec` (полоса). Выката нет.
Ветка — носитель переключения D: не вливать в `main` до D, и **только merge-коммитом** (squash или
rebase сотрут запись о `d0c9ec1`, и следующее слияние снова вынесет агентный слой).

## Что сделано

1. **Слияние в три шага вместо одного.**
   - `02b4769` — последний коммит MVP-1 перед выносом (только хендофф);
   - `git merge -s ours d0c9ec1` — вынос записан слитым, дерево не меняется;
   - `mvp1/main` (`d07f8a0`) — база теперь `d0c9ec1`.

   Зачем: при прямом слиянии, как было в плане, агентный слой теряет строки и в общих файлах, без конфликта.
   Возврат по списку путей Германа этого не видит.
2. **Конфликты — 12 файлов.** Взята версия MVP-1, агентные регистрации сохранены:
   - `config`, `background_jobs`, `cli`, `api` (применение итога), `docker-scheduler.sh` — обе
     стороны; `check-lanes` — последним шагом цикла;
   - `article_fetcher`, `request_parser`, `telegram_parser` — MVP-1: обе стороны чинили пустую
     страницу, у MVP-1 один шов — `normalize.parse_html`;
   - `api._external_worker_payload` — таблица сборщиков MVP-1, в неё добавлены радар и оценка кандидата;
   - `docker-compose.yml` — идентичность агентного стека оставлена, из MVP-1 взят `REQUEST_ARTICLE_LIMIT=12`;
   - `docker-compose.external-worker.yml` — раскладка NL из MVP-1 с его именами (цель D) и пятый
     сервис `external-worker-agents`.
3. **Возвращено вручную то, что пропуск `d0c9ec1` не спас.**
   - `b24adca` (MVP-1, уже после выноса) вычистил «хвосты агентного слоя»: 11 Pydantic-моделей и
     4 строки `SOURCE_DISCOVERY_*` в `.env.example`. Без моделей 13 агентных эндпоинтов отвечали 422:
     тело запроса FastAPI принимал за query-параметр. Модели возвращены байт в байт из `origin/main`.
   - `23fd7bd` (ссылки меню на поддомен) снят из `App.tsx` и `globals.css`. Тесты вернулись к родным
     экранам; правка закрытой регистрации (#33) оставлена.
4. **Полоса `external-agents`.**
   - Зачем: `lanes.check_enqueue` из MVP-1 отказывал радару и оценке кандидата в `external-ai`.
     Проверено красным тестом: «Очередь external-ai не обслуживает задачи вида 'signal_discovery'».
   - `lanes.route` переносит `signal_discovery` и `source_candidate_evaluate` из ИИ-полос в
     `external-agents`. Вызов стоит в `repository.create_background_job` — единственной точке вставки.
     Код Германа ставит задачи в `"external-ai"` строкой и не меняется.
   - Сторож: порог ожидания 120 мин; на NL — `nl-agents-1`.
5. **Смысловой конфликт, которого git не показал.**
   - `LeaseKeeper` из MVP-1 считает продвижением только `beat()`, а агентные обработчики звали голый heartbeat.
   - Последствие: прогон дольше 20 мин снимался бы как зависший (`os._exit(70)`, задача уходит в
     повтор, оплаченная работа выброшена). Максимум прогона радара на проде — уже 17 мин.
   - Теперь обработчики зовут `beat`, это закрыто тестом.

## Чем проверено

- **Прогон вхолостую**, `git merge-tree --write-tree`:
  - прямое слияние — 21 конфликт, 26 удалённых файлов и ~957 строк, молча вырезанных из общих файлов
    (`external_ai.py` 436, `test_cli.py` 318, `api/sources.ts` 159, `App.tsx` 44);
  - с пропуском `d0c9ec1` — 12 конфликтов, 0 удалений.
- `/Users/apple/Desktop/oiltech-digest/.venv/bin/python -m pytest tests/ -q -p no:cacheprovider` — **820 passed**.
  На промежуточном коммите слияния без полосы красных 5, список — в сообщении `7479463`.
- Фронт — временная ссылка `frontend/node_modules`, после проверки убрана:
  `npx tsc -b` чистый, `npm test` 36/36, `npm run build` проходит.
- **Имена тестов.** У `origin/main` 682, у `mvp1/main` 556, объединение — 818. В слиянии нет двух — тестов
  ссылок на поддомен из `23fd7bd`, сняты намеренно.
- **Граница Германа.** `git diff origin/main -- <пути>`: 38 из 39 файлов совпадают байт в байт, один
  отличается двумя строками (см. «Отступление»).
  Агентные блоки общих файлов дословно на месте:
  - `api.py` — 38;
  - `cli.py` — 28 блоков и 208 строк регистрации подкоманд;
  - `repository.py` — 55; единственное «изменение» — `digest_candidates` из ядра, там версия MVP-1;
  - `background_jobs.py` — 8, `config.py` — 18;
  - `docker-scheduler.sh` — все 90 агентных строк;
  - из `schema.sql` не удалено ни строки;
  - подкоманд CLI 94 — это объединение 89 и 68.
- **Схема против основной базы.**
  - С РФ снят `pg_dump --schema-only` (37 таблиц, 0 строк данных), восстановлен в локальную пробную базу.
  - `schema.sql` применён через `connection.init_db` дважды: без ошибок, идемпотентно.
  - Сравнение с пустой базой после того же `schema.sql`: колонок 452 = 452 (тип, NULL, умолчание),
    индексов 128 = 128. Ограничений 93 против 90 — см. ловушку про внешние ключи.
- **Дымовая проверка API.** База со слитой схемой, `TestClient` (https — кука сессии Secure):
  - вход — 200, 37 из 38 GET — 200; `/api/feedback` отвечает 400 без обязательного параметра;
  - `index.html` отдаётся, маршрутов `/api` — 95.
- **Замеры с РФ (только чтение).**
  - Радар: 12 успешных прогонов, медиана 4 мин, максимум 17, максимальное ожидание в очереди 133 мин.
  - Основная база: агентных задач в очереди нет (130 `failed` в `ai`, 257 `ok` в `default`).
  - Агентная база: на `signal_feedback_events` те же 3 внешних ключа, что в основной.

## Отступление, требующее согласия

`tests/test_signal_radar_robustness.py`, 2 строки. Файл наш (`2ac8ac1`, 21.09), но попадает в границу по
шаблону `test_signal_*`. Без правки план противоречит сам себе: «граница — ни байта», а в то же время
«радар — в новую полосу» и «весь набор зелёный».
- Первая строка: тест постановки берёт `scrape_source` в `external-fetch` — ровно так MVP-1 уже поправил
  двойник этого теста в `test_collection_robustness`.
- Вторая строка: радар ждётся в `external-agents`.
- Смысл обоих тестов («внешняя задача не исполняется на месте») сохранён.

## Не сделано намеренно

- Оценка кандидата на NL (`processing/external_ai.py`, `process_source_candidate_payload`) зовёт гейт
  релевантности без тематик заказчика, хотя `tags` в payload есть. Так было и до слияния. Решает Герман.
- CLAUDE.md по-прежнему описывает выкат агентного стека. Идентичность compose ядра (`agents-app` против
  `app`) — тоже задача D.
- Ревью PR — свежей сессией (`/code-review`), не автором.

## Ловушки и ложные версии

- **«Вернуть 22 файла Германа — и граница цела»** — ложно. Молча теряются и строки общих файлов, и
  «хвосты», вычищенные после выноса (`b24adca`). Проверка на будущее — удалённые строки против
  агентного `main`:
  `git diff -U0 origin/main -- . ':(exclude)docs/**' | grep '^-' | grep -iE 'signal|discover|candidate|agent|radar|brave|serpapi'`.
  Слово `candidate` в сборе — это `CandidateLink` ядра, шум.
- **422 вместо 200** — признак пропавшей Pydantic-модели: при `from __future__ import annotations`
  FastAPI молча делает тело query-параметром. Поймали только агентные тесты `test_api`.
- **`git merge -s ours` → «сбой при выполнении stash»** сразу после первого слияния. Причина — racy-индекс:
  файл и индекс записаны в одну секунду, и `git stash create` без обновления индекса видит изменения.
  Лечится `git update-index --refresh` перед следующим слиянием.
- **Два теста кодируют имя сервиса приложения в compose.** `test_collection_robustness` из MVP-1 ждал
  `app` — переписан без привязки к имени. `test_bare_compose_up_starts_only_core_services` (граница)
  ждёт `agents-app` — в D при смене идентичности его придётся менять.
- **Дрейф схемы.** У `signal_feedback_events` колонки `signal_id`, `signal_evidence_id`,
  `duplicate_of_signal_id` объявлены в `CREATE TABLE` без `REFERENCES`, а `ALTER … ADD COLUMN IF NOT
  EXISTS … REFERENCES` вешает ключ только на старых базах. Итог: в основной и агентной базах ключи есть,
  в базе с нуля — нет.
- **Выдача фильтрует только по очереди.** Застрявшая в `external-ai` старая задача радара уйдёт в
  `nl-ai-1` с сырым payload.
- **Инструменты.**
  - `preview_start` читает `launch.json` не из worktree — живую проверку делали через `TestClient`.
  - Сторож worktree отказывает сложным командам с git (подстановки, heredoc): проверки — скриптами
    из скретчпада.

## Следующий шаг

1. **Владелец:** согласовать отступление в `test_signal_radar_robustness.py` (при необходимости — с
   Германом); запустить `/code-review` по PR #1 в свежей сессии.
2. **D.1:** после B, C, F — обычный `git merge mvp1/main` в `merge/mvp1-core`: `d0c9ec1` уже записан.
   Сначала прогнать вхолостую (`git merge-tree --write-tree --name-only merge/mvp1-core mvp1/main`),
   после слияния — проверку удалённых агентных строк (команда выше) и `git diff origin/main -- <пути Германа>`.
   C трогает `external_worker.py` и `lanes.py` (SIGTERM, контракт версий) — ждать конфликтов там.
3. **Окно D:**
   - `nl-agents-1` поднять вместе с ядром (пока его нет, радар ждёт, а сторож пишет `no_consumer`);
   - на NL в `.env.external-worker` — `BRAVE_SEARCH_API_KEY`;
   - перед переключением проверить, что в `external-ai` нет агентных задач;
   - перенос в порядке `signals` → `signal_evidence` → `signal_feedback_events`;
   - переписать CLAUDE.md под единый контур.
