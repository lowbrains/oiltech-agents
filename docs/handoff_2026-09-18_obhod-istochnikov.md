# Хендоф 18.09.2026 — источники, парсер, агенты на поддомене

## ▶ НАЧАТЬ ОТСЮДА — для новой сессии

### Состояние на конец 18.09 (проверено замером, не по памяти)

| Контур | Версия | Состояние |
|---|---|---|
| РФ-ядро `109.68.213.12` | в образах код **`7d6b8e1`** (checkout `1119aa7`); `c858539` нужен только воркеру | 8 контейнеров MVP-1, health 200 |
| NL-воркер `85.234.107.233` | **`b2a5231`**, пересобран владельцем | новый код подтверждён: у задач сбора есть `skipped_known`, ошибок datetime 0 |
| Агенты **https://agents.oiltech-digest.ru** | образ `772a377`, main `lowbrains/oiltech-agents` = `3b5b636` | своя база `oiltech_agents`, сертификат, health 200; новые сигналы не генерируются |

`main` MVP-1 = `ce4e36c`, **474 теста**. Памяти на сервере доступно ~1 ГБ при 10 контейнерах.
Список задач — [`status_2026-09-17_spisok-zadach.md`](status_2026-09-17_spisok-zadach.md):
Сделано 12 · В работе 6 · Не начато 4 · Другой проект 10 · За владельцем 3.

### Приоритеты следующей сессии

1. **П.19 — анонс заказчику.** Был назначен на пт 18.09 и **не сделан**. Есть что показать:
   дедуп перепечаток (159 копий скрыто), обход 30 источников, починка парсера, радар на поддомене.
2. **П.27 — внести проверенные настройки источников** (таблица в разделе «Дополнение 18.09
   (день)» ниже): записи в `SOURCE_OVERRIDES` → `cli apply-source-overrides` → архив 5 →
   Bapco Energies через форму (заодно живая проверка перебора стратегий) → проверка
   **настоящим сбором**, не диагностикой.
3. **Предфильтр на NL без тематик заказчика** (с 17.09, `fffaee0`): `relevance_filter` читает
   тематики из базы, у воркера её нет. Передать тематики в payload задачи сбора, как у гейта.
4. **Старые данные:** 58 статей с кракозябрами и 42 «простыни» со скриптами — перекачать новым
   кодом (сайты с РФ доступны).
5. **Агенты:** второй NL-воркер (без него новых сигналов нет; токен —
   `/root/oiltech-agents/.worker-token`), решение по планировщику агентов (удвоит расход
   ~$74→$150/мес), пункты Германа 4 («один литий»), 12 (13 тегов), 11 (полный список полей),
   14 (скорость приёма ОС — не смотрел).
6. **Даты:** п.20 перетегирование 19–20.09 · п.21 решение по 5 электроэнергетическим до 20.09 ·
   п.22 оплата этапа 1 21.09 (владелец).
7. **Мелкое:** мост Telegram молчит с 16.09 (п.31) · судья перепечаток не пишет в
   `ai_processing_runs` · бэкап вынести с сервера · Energy Voice — выбрать законный канал ·
   Hart Energy и Saudi Aramco — законного пути нет.

### Как работать — выучено сегодня дорого

- **SSH до РФ** — только `ssh -o BindInterface=en0 root@109.68.213.12` (VPN режет пакеты).
  **До NL SSH нет ниоткуда** — докатывает владелец:
  `cd /root/oiltech-digest && git fetch origin && git reset --hard origin/main`, затем
  `docker compose -f docker-compose.external-worker.yml up -d --build`.
- **Деплой РФ без изменения схемы**: `docker compose build app tasks worker scheduler playwright-worker`
  + `up -d --no-deps` тех же; `bootstrap` не трогать (§10). Перед деплоем — нет ли `running` во
  внешних очередях: рестарт ядра рвёт lease.
- **`Caddyfile` смонтирован одним файлом** — после `git reset` грузить без простоя:
  `docker exec -i oiltech_caddy caddy reload --config /dev/stdin --adapter caddyfile < Caddyfile`.
- **Долгие операции на сервере** — через `nohup … > лог &`: SSH рвётся, и сборка умирает с ним.
- **Тест на синтетике — не доказательство.** Сегодня три мои правки прошли тесты и на проде не
  сработали или сломали его: кодировка (кириллица до `<meta charset>`), «Learn more»-карточки,
  datetime в JSON у воркера. Правку парсера — проверять новым кодом на живых лентах **до**
  записи в базу (§11.3); тест обязан падать без правки.
- **Настройки источников — только через `SOURCE_OVERRIDES`**: сидер перезаписывает `url` из xlsx.
- Где что: правила — [`architecture.md`](architecture.md) §11 · агенты —
  `oiltech-agents/CLAUDE.md` · память — `oiltech-sources-survey-0918`, `oiltech-parser-fix-0918`,
  `oiltech-agents-deploy-0918`.

---

# Часть 1. Обход 30 молчащих источников (пункты 26, 27)

Задача владельца: пройти все сайты самому, найти рабочие ссылки, «сколько найдём —
столько внесём», а как вносить — решить после. **В базе ничего не менялось.**
Временные скрипты на РФ-сервере (`/tmp/survey_*`, 6 файлов) удалены.

## Итог в одной строке

Из 30 «молчащих» **живых сайтов со свежими новостями — 22**. Новая ссылка лечит
11 из них; ещё 11 живы при уже правильной ссылке и молчат из-за маршрута или
парсера; 7 сайтов для дайджеста мертвы; Energy Voice закрыт интерактивной капчей.

## Как смотрел

Пять точек обзора — потому что сайты отвечают им по-разному:

| Точка | Адрес | Что это |
|---|---|---|
| `en0` | 109.252.162.105 | домашний провайдер, РФ |
| VPN | 185.120.76.63 | хостинг, Казахстан |
| VPN (переключил владелец) | 62.212.74.235 | LeaseWeb, Нидерланды — ближе всего к нашему воркеру |
| РФ-прод по SSH | 109.68.213.12 | так сайты видит парсер: curl + headless Chromium в `playwright-worker` |
| встроенный браузер | через VPN | настоящий браузер, как человек |

**Ловушка, на которую чуть не наступил:** Hart Energy с домашнего адреса отдал
441 КБ, а с прода — 403. «Открывается у меня» ≠ «открывается у парсера».

## Таблица

**A. Ссылка найдена или заменена — 11**

| id | Источник | Рабочая ссылка | Свежее | Замечание |
|---|---|---|---|---|
| 116 | ТПУ | `https://news.tpu.ru/news/` | 18.09 | в базе был `tpu.ru` — чужой хост, ссылки на поддомен парсер отсекал |
| 112 | Сколтех | `https://skoltech.ru/news` | 16.09 | есть нефтегаз: газопроницаемость гидратов |
| 98 | Белоруснефть | `https://www.belorusneft.by/ru/mediacenter/news/` | 17.09 | плитки `li.list-entry`, ссылка `/ru/detail-pages/event/-ГГГГ.ММ.ДД-…` **без текста**; curl не проходит устаревший TLS, Chromium проходит |
| 77 | ТеДо | `https://tedo.ru/press-center` | 14.09 | дата в адресе `news-ДДММГГГГ` |
| 118 | Минобрнауки | RSS `https://minobrnauki.gov.ru/press-center/news/rss/` | 15.09 | 40 пунктов, с прода отдаётся |
| 79 | Б1 | главная `https://b1.ru` → карточки `/analytics/…` | сент. 2026 | «Обзор нефтегазового рынка 1П 2026»; отдельного списка нет (`/analytics/` = 404) |
| 76 | Яков и Партнёры | `https://yakovpartners.ru/publications/?industiry=energy-oil-gas` | — | «Рынок нефтесервиса до 8,4 трлн к 2035»; **дат на сайте нет вообще** |
| 23068 | PETRONAS | `https://www.petronas.com/media/media-releases` | 15.09 | нынешний RSS `/rss.xml` мёртв с 07.05.2025 |
| 25 | Weatherford | `https://www.weatherford.com/investor-relations/investor-news-and-events/news/` | 03.09 | NCS Multistage, ExxonMobil Nigeria; curl 403 даже из NL, браузер из NL проходит |
| 17 | S&P Global | `https://www.spglobal.com/energy/en/news-research/latest-news` | 18.09 | бренд переименован в S&P Global Energy, старый адрес ведёт на лендинг; вперемешку всё сырьё |
| 85 | IHS / S&P | — | — | **дубль id 17**, тот же адрес |

**B. Ссылка уже верна — молчит по другой причине — 11**

| id | Источник | Свежее | Почему молчит |
|---|---|---|---|
| 64 | QatarEnergy | 10.09 | режет РФ-адреса (403 с дома и с прода), из KZ и NL — 200; не опрашивается с 17.06 |
| 16 | Wood Mackenzie | 17.09 | curl из РФ/KZ получает стену; **из NL — 200 обычным запросом** |
| 26 | TechnipFMC | 17.09 | curl — 403 везде, включая NL; настоящий браузер проходит |
| 21 | OPEC | 06.09 | то же: Cloudflare для curl везде, браузер проходит |
| 72 | Shell | 10.09 | список рисуется ~6 с, браузер парсера ждёт 3,5 с (`settle_ms=3500`) |
| 67 | Kuwait Oil | 24.08 | список SharePoint, рисуется скриптом |
| 63 | Mubadala | 02.09 | с прода 200, ссылка та же — парсер отбрасывает, причина не разобрана |
| 24392 | JPT R&D | свежее | настроенный селектор находит 12 статей — парсер отбрасывает на шаге статьи; часть материалов `-restricted` |
| 9 | Hart Energy | 14.09 | **режет любой дата-центр** (РФ-прод и NL — 403), пускает только домашние адреса; RSS 403 даже с дома |
| 61 | Saudi Aramco | 24.08 | то же: из дата-центров нет соединения, из домашних сетей открывается; лента `/en/news-media/news` рисуется скриптом; не опрашивался ни разу |
| 86 | Spears & Associates | 25.06 | **не сломан** — пишет раз в 1–2 месяца |

**C. Вносить нечего — 7**

| id | Источник | Почему |
|---|---|---|
| 48 | ЦДУ ТЭК | последняя новость **29.01.2025** (дата из самой статьи) — ответ на п.26 |
| 100 | KazPetroDrilling | последняя новость 05.05.2022 |
| 68 | Tatweer | последняя 05.09.2023; компания переименована в Bapco Upstream. **Замена:** `https://www.bapcoenergies.com/media-centre/press-releases` — живая, ежемесячно, есть фильтр по дочкам |
| 44 | Сургутнефтегаз | свежие релизы — только предупреждения о мошенниках; с прода таймаут; редирект на себя через cookie |
| 108 | РАН | в ленте административное (конкурсы, нормативы), последнее 27.05 |
| 23058 | PetroChina | ~4 релиза в год (30.08 — итоги полугодия); curl 412 везде, браузер проходит |
| 23060 | CNOOC | часто, но партийная учёба; curl — JS-заглушка, браузер проходит |

**Energy Voice (id 10)** — отдельно. И из KZ, и из NL Cloudflare Turnstile
требует нажать «Подтвердите, что вы человек». RSS, лента раздела и даже
`sitemap.xml` — 403. Капчу я не прохожу: это обход защиты от ботов, запрещено
мне и по просьбе. Сайт жив: Google News RSS по `site:energyvoice.com` — 100
пунктов за 7 дней, но примерно у половины **пустой заголовок**, текста нет.
Законные варианты: Google News RSS (неполно), почтовая рассылка издания в наш
ящик, запрос издателю (DC Thomson) на доступ.

## Что обход сказал про парсер (для решения «как»)

Это гипотезы из кода, подтверждённые на примерах, но **не прогоном парсера** —
проверять перед правкой.

1. **Выбор кандидатов.** Сортировка по баллу, при равном — **по алфавиту адреса**,
   затем первые 6 (`REQUEST_ARTICLE_LIMIT=6`). Порядок страницы (свежее сверху)
   выбрасывается; дата из `article_date_selector` в сортировке не участвует.
   Закреплённые пункты занимают места навсегда. Касается всех 88 `request` и
   `playwright` источников.
2. **Ссылки без текста отбрасываются** (заголовок < 18 знаков) — даже с
   селектором. Так устроены Белоруснефть, Б1, «Яков и Партнёры».
3. **Только тот же хост** — новости на поддомене (`news.tpu.ru`) отсекаются.
4. **Дата из статьи не извлекается** на ЦДУ ТЭК, Сколтехе, ТеДо; у ТПУ даты в
   статье нет вообще, она только в списке. Отсюда пустые `published_at`.
5. **Браузер парсера ждёт 3,5 с** — Shell рисуется ~6 с.
6. **Mubadala и JPT** отбрасываются на шаге статьи — причина не разобрана.

## Что оказалось не тем, чем выглядело

- **«30 из 31 молчащих без селектора — один корень»** (мой вывод 17.09) — ошибка
  по базовой доле: без селектора **115 из 124** активных. Реальный сигнал слабее:
  у `request` без `listing_url` молчат 38% против 12% (p = 0,055, точный тест Фишера).
- **Эти источники не «перестали работать» — они не работали никогда.** «Уловы»
  30.05, 27.06 и 22.07 одни и те же у всех: 22.07 — разовая перезаливка старых
  страниц после смены правила финальной косой черты (~20.07), 30.05 — первичный
  засев. У ЦДУ ТЭК все 16 «статей» — декабрь 2024 и их дубли; у Сургутнефтегаза —
  «История дивидендов», «Закупки», «Банковские реквизиты».
- **VPN владельца выходит не в Австрии, а в Казахстане** (июльская запись памяти устарела).

## Пункт 33 — бэкап доказан

Первый автозапуск 18.09 03:20 UTC: `ok 2026-09-18 03:20:28: дамп 243571 строк, 72M`,
дамп 75 МБ + документы 10 МБ. Открытым остаётся только вынос копии с того же сервера.

## Решения, которые нужны от владельца

1. **A (11):** вносить ссылки сразу — или сначала чинить пункты 1–3 парсера?
   Без них часть источников даст один улов и снова замрёт (Белоруснефть и Б1 —
   гарантированно, из-за ссылок без текста).
2. **B (11):** маршрут через NL для QatarEnergy и Wood Mackenzie — дёшево;
   браузер на воркере для TechnipFMC, OPEC, S&P, Weatherford — работает ли
   headless Chromium воркера, не проверено.
3. **Hart Energy и Saudi Aramco** режут дата-центры — ни один наш воркер их не видит.
4. **C (7):** архивировать? Tatweer — заменить на Bapco Energies?
5. **Energy Voice:** какой из законных вариантов.

Память: `oiltech-sources-survey-0918`.

---

# Дополнение 18.09 (день): починка парсера по итогам обхода

Решение владельца: «чиним всё; ссылки расширяем внутри источника; проверь ручной
ввод ссылки — пробуется ли каждая стратегия». Сделан код, выкачен на РФ-ядро,
проверен на живых лентах. **Настройки источников в базу НЕ внесены, NL-воркер НЕ
пересобран** — ниже что и как докатить.

## Что сделано (выкачено на РФ-ядро, прод = `7d6b8e1`)

| Коммит | Что |
|---|---|
| `2bac79c` | выбор ссылок: свежее первым вместо «старые/по алфавиту», лимит 6→12, заголовок из карточки, поддомены, селектор главнее фильтра мусора, даты всех встреченных форм; пара попыток браузера общей функцией (РФ, NL, диагностика); heartbeat и `known_urls` для NL; перебор стратегий при ручном вводе; держатель импорта выключен |
| `e75564e` | кодировка: декодировать самим — libxml2 не видит `<meta charset>` после кириллицы |
| `b57c52b` | заголовок карточки «Learn more» (Mubadala) |
| `cb2f3a2` | запасной текст статьи без `<script>`/`<style>` |
| `7d6b8e1` | относительные ссылки от конечного адреса ленты (`<base href>`), слитная дата `t20260914` |

Тесты: **473 зелёных**, 23 новых — каждый проверен на падение без своей правки.
Правила — `docs/architecture.md` §11.3–§11.4.

## Ответ владельцу на «пробуется ли каждая стратегия при ручном вводе»

**До правки — нет.** Форма добавления источника искала только RSS, а без него молча
ставила `request` по введённому адресу: без проверки, что он даёт статьи, без
браузера, без маршрута через NL. Так и заводились источники, не давшие ни одной
статьи никогда. **Теперь** `probe_strategies`: RSS (и проверка, что лента не мёртвая,
>90 дней) → запрос → браузер; с РФ закрыт — сбор через NL; ничего не нашлось —
источник заводится **выключенным**, отчёт перебора в ответе API. Покрыто тестами,
**вживую на проде ещё не пробовалось** — план: завести так Bapco Energies.

## Проверено новым кодом на проде (только чтение) — готово к внесению

| Источник (id) | Настройка | Итог проверки |
|---|---|---|
| ТПУ (116) | `request`, listing `https://news.tpu.ru/news/` | 12 кандидатов, все с датой, статья 3929 зн. |
| Сколтех (112) | `request`, listing `https://skoltech.ru/news`, селектор `a[href*="/news/"]` | 12, статья 5264 зн. (дат в ленте нет) |
| Белоруснефть (98) | `request`, listing `https://www.belorusneft.by/ru/mediacenter/news/`, селектор `li.list-entry a[href*="/detail-pages/"]` | 12, все с датой, статья 1410 зн. |
| ТеДо (77) | `playwright`, listing `https://tedo.ru/press-center`, селектор `a[href*="/press-center/news-"]` | 12 с датой, статья 9465 зн. |
| Минобрнауки (118) | `rss`, `https://minobrnauki.gov.ru/press-center/news/rss/` | 40 пунктов, свежее 15.09 |
| Б1 (79) | `request`, listing `https://b1.ru`, селектор `a[href*="/analytics/"]` | 6, статья 5936 зн. (дат нет) |
| Яков и Партнёры (76) | `request`, listing `https://yakovpartners.ru/publications/?industiry=energy-oil-gas`, селектор `a[href*="/publications/"]:not([href*="industiry"])` | 10, статья 5530 зн. (дат нет) |
| Mubadala (63) | + селектор `a[href*="/news/"]` | 12, 10 с датой, свежее 02.09 |
| CNOOC (23060) | `playwright`, listing `https://www.cnooc.com.cn/zxzx/gsxw/`, маршрут **auto** (с РФ работает) | 10 с датой, 14.09, статья 1140 зн. |
| Shell (72) | без изменений | работает с новым кодом: 12 с датой, 10.09, статья 11 тыс. зн. |

С РФ не проверить — только после пересборки NL: **Weatherford (25)** — `playwright`,
external, listing `https://www.weatherford.com/investor-relations/investor-news-and-events/news/`,
селектор `a[href*="news-article"]`; **S&P Global (17)** — url `https://www.spglobal.com/energy`,
`playwright`, external, listing `https://www.spglobal.com/energy/en/news-research/latest-news`;
**QatarEnergy (64), Wood Mackenzie (16), TechnipFMC (26), OPEC (21), Kuwait Oil (67)** —
`network_region=external` (Kuwait с РФ отдаёт урезанную страницу); **PetroChina (23058)** —
`playwright`, external, listing `https://www.petrochina.com.cn/petrochina/xwxx/xwgg_list.shtml`
(с РФ ноль — антибот).

**В архив:** ЦДУ ТЭК (48), KazPetroDrilling (100), Tatweer (68), PETRONAS (23068 —
дубль: ленту релизов уже собирает «Petronas» id 65), IHS/S&P (85 — дубль 17).
**Новый:** Bapco Energies — через форму добавления (заодно живая проверка перебора).
**Не трогать:** Hart Energy и Saudi Aramco (режут любой дата-центр), Energy Voice
(капча), Spears (пишет редко), JPT R&D (работает — его статьи раньше собирают соседние
разделы JPT: 4 из 6 проверенных уже в базе у них), Сургутнефтегаз, РАН.

## Как вносить (следующая сессия)

1. Записи в `SOURCE_OVERRIDES` (`ingestion/source_overrides.py`) с `source_type` —
   **не прямым UPDATE**: сидер при каждом bootstrap перезаписывает `url` из xlsx.
2. `docker compose exec -T app python -m oiltech_digest.cli apply-source-overrides` —
   только UPDATE источников, схему не трогает. Планировщик сам их не применит:
   `SKIP_BOOTSTRAP=1`.
3. Архив — `POST /api/sources/{id}/archive` (сидер его не откатывает).
4. Проверка каждого — настоящий сбор, не диагностика: статьи легли, даты есть,
   заголовки без кракозябр, не навигация.

## Докатить на NL-воркер (нужен владелец: SSH до `85.234.107.233` не проходит ни с Mac, ни с РФ, ни через NL-VPN)

```bash
cd /root/oiltech-digest && git fetch origin && git reset --hard origin/main
docker compose -f docker-compose.external-worker.yml up -d --build
docker compose -f docker-compose.external-worker.yml logs --tail 20 external-worker
```

Путь `/root/oiltech-digest` — по аналогии с РФ, проверить. **Как понять, что доехало:**
в `result_json` задачи `scrape_source` из очереди `external-*` появляется поле
`stats.skipped_known` — у старого кода его нет.

До пересборки на NL работает старый парсер, а ядро уже шлёт ему лимит 12. Риск —
lease у долгих `request`-задач без heartbeat; следить за `attempts > 1` у `external-fetch`.

## Что оказалось не тем, чем выглядело (сегодня)

- **Две мои правки прошли тесты и не сработали на проде.** Кодировка: тест брал
  страницу без объявления кодировки, а у Сколтеха она объявлена — но после кириллицы
  в `<title>`, и libxml2 её не видит. Mubadala: заголовок в «Learn more»-карточке не
  находился. Поймала только проверка на живых лентах — отсюда правило в §11.3.
- **Тест на скрипты сначала проходил и на старом коде**: фикстура стояла ровно на пороге
  120 знаков. Проверка «падает без правки» сработала — исправил фикстуру.
- **«JPT R&D — парсер отбрасывает»** (из обхода) — неверно: статьи уже собраны соседними
  разделами JPT.
- **«PETRONAS — новая ссылка»** (из обхода) — неверно: её уже собирает id 65; завёл бы дубль.
- **«Парсер ждёт 3,5 с»** — неточно: на РФ список ждал 5 с и 12 с повтором; 3,5 с — только
  страница статьи; без повтора был именно NL-воркер.

## Незакрытое по пункту 27

1. Пересборка NL-воркера (владелец).
2. Внесение проверенных настроек + архив + Bapco (таблица выше) и проверка сбором.
3. Починка существующих данных: **58 статей с кракозябрами** и **42 статьи-«простыни»**
   (перекачать новым кодом — сайты с РФ доступны).
4. Законный канал для Energy Voice (Google News RSS / рассылка / издатель) — решение владельца.
5. Hart Energy и Saudi Aramco — режут дата-центры; законного автоматического пути нет.
6. Судья перепечаток не пишет в `ai_processing_runs` (из 17.09, не трогал).

Память: `oiltech-parser-fix-0918`.

---

# Дополнение 18.09 (после обеда): регрессия NL, поддомен агентов

## Регрессия моей правки — найдена и закрыта

Через час после пересборки NL внешние задачи сбора падали «Object of type datetime is
not JSON serializable»: правка дат (`2bac79c`) дала кандидатам даты, а
`last_seen_published_at` уходил в ответ ядру объектом datetime. Мои тесты брали
кандидатов без дат. Фикс `c858539` + тест с `json.dumps`. Владелец пересобрал NL второй
раз — **проверено: после 11:59 UTC 16 задач `ok`, ошибок datetime 0, `skipped_known` у 11**
(остальные 5 — RSS, у них этого поля нет) — новый код на воркере.

## Поддомен агентов работает: https://agents.oiltech-digest.ru

Своя база `oiltech_agents` (отдельный Postgres, корпус дампом — счётчики один в один),
DNS добавлен владельцем, сертификат выпущен, отдаётся фронтенд агентов с новым радаром.
Основной сайт не задет. Подробно — `oiltech-agents/CLAUDE.md`. Две ловушки:

- **Два «app» в одной сети.** Compose даёт контейнеру имя сервиса псевдонимом в каждой
  сети — подключи я приложение агентов к сети MVP-1 под именем `app`, Caddy раскидывал бы
  запросы заказчика между MVP-1 и агентами. Сервис назван `agents-app`, база — по имени
  контейнера; проверено `getent hosts app` из Caddy — один адрес.
- **`Caddyfile` смонтирован одним файлом**: после `git reset` контейнер видит старую копию.
  Загрузка без простоя — `caddy reload --config /dev/stdin` с новым файлом на входе.

Осталось для живой генерации сигналов: второй NL-воркер для агентов (токен в
`/root/oiltech-agents/.worker-token`) и планировщик агентов (удвоит расходы — решение владельца).

## Найдено попутно, не чинил

**Предфильтр на NL-воркере работает без тематик заказчика.** `relevance_filter` читает
`list_enabled_tags()` из базы, у воркера базы нет — в логе «не удалось прочитать тематики
для предфильтра». Затронуты все внешние источники с 17.09 (`fffaee0`). Тот же класс, что
тематики гейта 17.09: лечится передачей тематик в payload задачи сбора.
